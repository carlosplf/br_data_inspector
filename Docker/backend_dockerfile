FROM node:12-alpine
RUN apk add --no-cache python3-dev py3-setuptools  make build-base linux-headers pcre-dev
WORKDIR /br_data_inspector
COPY ../ .
RUN pip3 install -r requirements.txt
CMD ["uwsgi", "--socket", "0.0.0.0:8080", "--protocol=http", "-w", "wsgi:app"]

